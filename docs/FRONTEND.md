# Frontend Architecture

N/A — Rediscover is a CLI-only system. All interaction is via terminal and file-based outputs (results.tsv, experiment_log.md, git log).

A monitoring dashboard may be added later if needed. If so, this document will be updated with the frontend architecture.
